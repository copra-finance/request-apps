{
  "name": "request-apps",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/shared",
      "packages/request-ui",
      "packages/create",
      "packages/pay"
    ]
  },
  "scripts": {
    "start:pay": "yarn --cwd packages/pay start",
    "start:create": "yarn --cwd packages/create start",
    "dev": "npm-run-all --parallel start:pay start:create"
  },
  "devDependencies": {
    "yarn-run-all": "^3.1.1"
  }
}
